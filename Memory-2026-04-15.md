- Correction learned for `kelvinCB/Agents-Mission-Control`: Etiven daily memory files must NOT be placed at repo root. Correct location is `data/memory/Etiven/Memory-YYYY-MM-DD.md`. Existing misplaced root `Memory-*.md` files need to live under `data/memory/Etiven/`.
- Correction learned for `kelvinCB/Agents-Mission-Control`: Etiven daily memory files must NOT be placed at repo root. Correct location is `data/memory/Etiven/Memory-YYYY-MM-DD.md`. Existing misplaced root `Memory-*.md` files need to live under `data/memory/Etiven/`.
- OpenClaw post-update/security finding: `trustedProxies` warning is only relevant if Control UI is behind a reverse proxy; otherwise low priority. The more important warning is multi-user/attack-surface posture caused by open Telegram group policy (`groupPolicy=open` and `groupAllowFrom=["*"]`). Recommended fix is minimum-hardening: switch Telegram groups to `allowlist` and remove wildcard `groupAllowFrom`.

- 2026-04-15 19:00 Europe/Berlin: Cron reminder captured recurring WhatsApp gateway flaps for +18299698254 with repeated disconnect status 499 roughly every 30 minutes from 14:47 to 18:47, each followed by reconnect within seconds. Reminder also included scheduled text: "Buenos días Alex ☀️". Duplicate reminder payload received multiple times in-session; handled internally and not relayed.
