# 2026-04-13

- 19:00 Europe/Berlin: Cron reminder captured repeated WhatsApp gateway flaps for +18299698254, disconnecting with status 499 roughly every 60 seconds and immediately reconnecting.
- Ran `openclaw status` and `openclaw doctor --non-interactive`: gateway service was running; WhatsApp still showed linked/OK despite the flap pattern.
- Inspected recent logs with `openclaw logs --plain --limit 300 --local-time | grep -Ei 'whatsapp|499|disconnect|connected as'`.
- Key finding: every reconnect is preceded/followed by `restored corrupted WhatsApp creds.json from backup` for `/home/kelvin/.openclaw/credentials/whatsapp/default/creds.json`.
- Working diagnosis: unstable/corrupted WhatsApp credential state (or another process repeatedly touching that creds file), not just a transient network drop.
- No destructive action taken. Did NOT delete credentials, relink account, or restart services blindly.
- Suggested next manual remediation if issue persists when Kelvin checks: backup credentials dir, stop gateway, inspect/replace `creds.json` + backups, then relink WhatsApp cleanly.
