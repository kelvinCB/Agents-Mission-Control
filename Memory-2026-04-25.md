
- 2026-04-25 19:00 Europe/Berlin: Cron reminder captured continued WhatsApp gateway flaps for +18299698254 with disconnect status 499 every ~30 minutes from 14:52 to 18:53, each followed by reconnect within seconds. Investigated locally: `openclaw status` shows WhatsApp channel currently OK, but `openclaw logs` reveals repeated warning `restored corrupted WhatsApp creds.json from backup` at every reconnect interval, strongly suggesting recurring credential/session file corruption rather than random network-only drops. Reminder also included scheduled text: "Buenos días Alex ☀️". Duplicate reminder payload received multiple times in-session; handled internally and not relayed.
