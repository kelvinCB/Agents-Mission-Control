# 2026-04-07

- 19:00 Europe/Berlin: Cron reminder surfaced repeated WhatsApp gateway reconnect loop (~18:43-18:52 local), disconnecting every ~60s with status 499.
- Ran `openclaw status` and `openclaw gateway status`: WhatsApp channel reported ON/OK and linked after the loop.
- Checked `openclaw logs --plain --limit 120 --local-time`: loop was caused by `web-heartbeat` forcing reconnects after >30 minutes with no inbound messages (`Message timeout detected - forcing reconnect`). Each reconnect also logged `restored corrupted WhatsApp creds.json from backup` for `/home/kelvin/.openclaw/credentials/whatsapp/default/creds.json`.
- At 18:53 local the pattern stopped; subsequent logs show stable `web gateway heartbeat` entries without forced reconnects. No user-facing alert sent because reminder explicitly said handle internally.
- Follow-up idea if it happens again: inspect/backup creds dir and consider restarting gateway during the loop to clear stale session state.
