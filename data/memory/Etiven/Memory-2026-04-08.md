# 2026-04-08

- Investigated repeated WhatsApp gateway disconnects (`status 499`) seen around 15:18–15:27 Europe/Berlin.
- `openclaw status` at ~19:00 Berlin showed WhatsApp channel currently `OK` and linked as `+18299698254`.
- `openclaw logs` showed a pattern:
  - repeated `web reconnect: connection closed` with `status=499`
  - immediate successful reconnect scheduling
  - repeated warnings: `restored corrupted WhatsApp creds.json from backup`
- Signals suggest the channel recovered, but there may be intermittent credential/session file corruption or churn at `/home/kelvin/.openclaw/credentials/whatsapp/default/creds.json`.
- As of investigation time, heartbeats were normal again and reconnectAttempts were `0` on the active connection.
- Recommended next follow-up if issue repeats: inspect credential file durability / backup restore path, and consider controlled WhatsApp relink only if delivery symptoms appear.
