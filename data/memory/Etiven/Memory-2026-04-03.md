# 2026-04-03

- Cron reminder captured repeated WhatsApp gateway connect/disconnect events (`status 499`) around 00:36–00:45 Europe/Berlin.
- Internal check run at ~19:00 Europe/Berlin:
  - `openclaw status`: gateway service running, WhatsApp channel currently `OK` and linked as `+18299698254`.
  - `openclaw doctor --non-interactive`: no direct WhatsApp outage, but unrelated security findings present.
  - `openclaw logs`: recurring warning found — `restored corrupted WhatsApp creds.json from backup` for `/home/kelvin/.openclaw/credentials/whatsapp/default/creds.json`.
- Pattern: the service recovers automatically, but the recurring credentials corruption/restore cycle is the most likely root cause behind the reconnect churn.
- Suggested next diagnostic when needed: inspect filesystem/disk stability around that creds path, backup/restore timing, and whether another process is touching the WhatsApp credentials directory.
