# 2026-07-03

- OpenClaw resumed after a gateway restart, but the visible current session only contained the restart notice and no prior user task or tool output.
- `memory_search` was unavailable because the memory index metadata is missing or incompatible. Suggested maintenance command: `openclaw memory index --force`.
